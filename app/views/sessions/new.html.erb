<%= notice %>
<%= form_with(url: sessions_path, class: 'flex flex-col gap-y-2') do |f| %>
  <%= f.password_field :password, class: 'input', placeholder: 'Enter password' %>
  <%= f.submit "Sign in", class: 'button' %>
<% end %>
